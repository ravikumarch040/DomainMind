FROM python:3.11-slim AS runtime
WORKDIR /app
RUN pip install --no-cache-dir fastapi uvicorn httpx pydantic sse-starlette
COPY services/orchestrator/ orchestrator/
ENV PYTHONPATH=/app
EXPOSE 8003
CMD ["uvicorn", "orchestrator.main:app", "--host", "0.0.0.0", "--port", "8003"]
